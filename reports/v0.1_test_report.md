# Report test v0.1

Data: test manuale eseguito su Windows CMD.

Ambiente verificato:

```text
Python 3.12.2
Windows CMD
Repository estratta in Downloads
```

## Test eseguiti

### 1. ReAct / agenti / tool use

Comando:

```bat
python scripts\search.py "ReAct agenti tool use"
```

Esito: OK.

Risultati attesi e osservati:

- Google - ReAct
- Anthropic - Uso degli strumenti
- OpenAI - Preamble for tool-heavy tasks
- Perplexity - Agentic actions
- Anthropic - Sistemi agentici e stato

### 2. OpenAI / retrieval / grounding

Comando:

```bat
python scripts\search.py "retrieval budget citazioni grounding"
```

Esito: OK.

Risultati principali:

- OpenAI - Retrieval budget
- OpenAI - Grounding and citations

### 3. Claude / XML / long context

Comando:

```bat
python scripts\search.py "XML documenti lunghi contesto"
```

Esito: OK.

Risultati principali:

- Anthropic - Tag XML
- Anthropic - Prompting con contesto lungo

### 4. Google / configurazione modelli

Comando:

```bat
python scripts\search.py "temperature top K top P output length"
```

Esito: OK.

Risultato principale:

- Google - LLM output configuration

### 5. Perplexity / Spaces

Comando:

```bat
python scripts\search.py "Spaces knowledge base brand consistency"
```

Esito: OK.

Risultato principale:

- Perplexity - Perplexity Spaces

### 6. Filtro vendor Anthropic

Comando:

```bat
python scripts\search.py "tool use agents" --vendor anthropic
```

Esito: OK.

Risultati solo vendor `anthropic`.

### 7. Filtro vendor Perplexity

Comando:

```bat
python scripts\search.py "Comet Research Labs Spaces" --vendor perplexity
```

Esito: OK.

Risultati solo vendor `perplexity`.

## Conclusione

La v0.1 e validata come repository seed interrogabile.

Funziona:

- ricerca locale su `chunks.jsonl`
- ranking semplice ma coerente
- filtri vendor
- recupero multi-fonte

Non ancora incluso:

- sintesi automatica finale
- ingestion automatica PDF/link
- BM25 reale
- vector search
- LLM collegato
- verifica automatica grounding
