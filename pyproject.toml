[project]
name = "ai-docs-repository"
version = "0.1.3"
description = "Lightweight Prompting documentation knowledge base with JSONL chunks, simple search, and evidence packs."
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100
